# Local Input Data

Place the Case1354 input workbook in this directory for a portable local setup, or set `config.dataFile` to its actual location. Excel data files are intentionally ignored by Git.
