# Generated Results

MATLAB writes Excel, MAT, JSON, and CSV outputs here when configured to use the repository-local output directory. Routine generated results are ignored by Git.

`main/` is an intentionally versioned snapshot produced by the primary 24-hour market model. It contains the Excel result workbook, the MATLAB result structure, run configuration, input/code manifest, and hourly run log used for the shared project example.
